language: python

python:
 - pypy
 - pypy3
 - 2.7
 - 3.6
 - 3.5
 - 3.4

sudo: false

install:
 - pip install pep8 pyflakes

script:
 # Pretend tests
 - pyflakes .
 - pep8 --statistics --count .

matrix:
  fast_finish: true
